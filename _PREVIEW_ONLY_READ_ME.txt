index.html, _redirects, vercel.json are PREVIEW-ONLY (so the base URL lands on
the home page when hosted on Vercel/Netlify). Do NOT upload these three to 20i —
the live domain root is the existing WordPress site. On 20i, upload only the page
folders: lp/, selling-inherited-property/, important-advice/, why-us/, faqs/, contact/.
The *.py / *.md build tools under lp/shared/ also don't need uploading.
