# Proxy the lead API through the site so the frontend can use a same-origin
# path (/api/lead) instead of a hardcoded backend URL — identical on preview
# and live, no CORS. Status 200 = proxy (rewrite), not a redirect.
/api/*  https://ppb-api-lnt0y.sevalla.app/:splat  200
